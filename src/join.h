#ifndef JOIN_H
#define JOIN_H

void create_join_database(employee_data*, uint32_t);
void run_join_operation(trip_data*, uint32_t);
void print_joined_database(uint32_t);
void cleanup_joined_database(uint32_t);

#endif

